describe('입력한 이메일과 비밀번호를 제출을 하면 회원가입이 완료된다.', () => {
  it('회원가입 api가 요청되면, 로더가 노출된다.', () => {
    // Given
    // When
    // Then
  });
  it('회원가입 api의 status가 200이 내려오면, 회원가입 완료페이지가 노출된다.', () => {});
  it('회원가입 api의 status가 400이나 500이 내려오면, 실패페이지가 노출된다.', () => {});
});
